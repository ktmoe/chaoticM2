import 'package:flutter/material.dart';

abstract class Styles {
  static const m2TextTheme = TextStyle(color: Color(0x99000000), height: 2);

  static const m2ButtonShape = RoundedRectangleBorder(
      borderRadius: BorderRadius.all(Radius.circular(5)));
}
